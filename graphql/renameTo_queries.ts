// here will go all the queries you have in stepzen when you export the querie itself

/** similar like this one:
 * 
 * import { gql } from '@apollo/client';
 * 
 * export const GET_DATA = gql`
  query GetSomeData   {
    getSomeData {
      value {
        
      }
      id 
    }
  }
`;
 */